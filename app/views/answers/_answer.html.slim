p= answer.body
- if current_user&.author_of?(answer)
  p= link_to 'Delete answer', question_answer_path(@question, answer), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
